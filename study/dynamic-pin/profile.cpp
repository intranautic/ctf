// pin tool which profiles the process
#include "pin.H"

KNOB<std::string> log_file(KNOB_MODE_WRITEONCE, "pintool", "o", "profile.log", "output file");


int main(int argc, char** argv) {

  return 0;
}
